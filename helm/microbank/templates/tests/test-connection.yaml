apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microbank.fullname" . }}-test-connection"
  labels:
    {{- include "microbank.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microbank.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
