apiVersion: v1
kind: Service
metadata:
  name: {{ include "microbank.fullname" . }}
  labels:
    {{- include "microbank.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "microbank.selectorLabels" . | nindent 4 }}
